function n = inter(c1, c2)
    n = 0;
    for i=1:length(c1)
        for j=1:length(c2)
            if c1(i,1) == c2(j,1) && c1(i,2) == c2(j,2)
               n = n + 1;
            end
        end
    end
